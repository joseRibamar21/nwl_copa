export interface UserI {
  name: string;
  email: string;
  avatarUrl: string;
  wallet: number
}

export type User = {
  id: string
  name: string;
  email: string;
  avatarUrl: string;
  wallet: number;
}
